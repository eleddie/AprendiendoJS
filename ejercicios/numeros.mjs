/**
 * 1. Crear una función que reciba un número y retorne true si es par y false si es impar.
 */

function esPar(numero) {}

console.log(esPar(2));

/**
 * 2. Crear una función que reciba un número y retorne true si es primo y false si no lo es.
 */

function esPrimo(numero) {}

console.log(esPrimo(2));

/**
 * 3. Crear una función que reciba un número retorne y true si es positivo y false si es negativo.
 */

function esPositivo(numero) {}

console.log(esPositivo(2));

/**
 * 4. Crear una función que reciba un número y retorne true si es divisible por 3 y false si no lo es.
 */

function esDivisiblePor3(numero) {}

console.log(esDivisiblePor3(3));

/**
 * 5. Crear una función que reciba un número y retorne true si es capicúa y false si no lo es.
 */

function esCapicua(numero) {}

console.log(esCapicua(12321));

/**
 * 6. Crear una función que reciba un número y retorne el factorial de ese número.
 */

function factorial(numero) {}

console.log(factorial(5));

/**
 * 7. Crear una función que reciba un número y retorne la suma de sus dígitos.
 */

function sumaDigitos(numero) {}

console.log(sumaDigitos(123));

/**
 * 8. Crear una función que reciba un número y retorne el numero invertido.
 */

function invertirNumero(numero) {}

console.log(invertirNumero(123));

/**
 * 9. Crear una función que reciba un número y retorne true si es un número de Fibonacci y false si no lo es.
 */

function esFibonacci(numero) {}

console.log(esFibonacci(5));

/**
 * 10. Crear una función que reciba un número y retorne el valor en binario.
 */

function binario(numero) {}

console.log(binario(5));

/**
 * 11. Crear una función que reciba un número y retorne true si esta dentro de un rango y false si no lo está.
 */

function estaEnRango(numero, rango) {}

console.log(estaEnRango(5, [1, 10]));
