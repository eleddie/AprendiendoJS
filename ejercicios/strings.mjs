/**
 * 1. Crear una función que reciba una cadena de texto y retorne la cadena invertida.
 */

function invertirCadena(cadena) {}

console.log(invertirCadena("Hola"));

/**
 * 2. Crear una función que reciba una cadena de texto y retorne la cadena en mayúsculas.
 */

function mayusculas(cadena) {}

console.log(mayusculas("Hola"));

/**
 * 3. Crear una función que reciba una cadena de texto y retorne la cadena en minúsculas.
 */

function minusculas(cadena) {}

console.log(minusculas("Hola"));

/**
 * 4. Crear una función que reciba una cadena con espacios al principio y al final y retorne la cadena sin espacios.
 */

function sinEspaciosAmbosLados(cadena) {}

console.log(sinEspaciosAmbosLados("   Hola   "));

/**
 * 5. Crear una función que reciba una cadena de texto y retorne la cantidad de palabras que contiene.
 */

function cantidadDePalabras(cadena) {}

console.log(cantidadDePalabras("Hola Mundo Loco"));

/**
 * 6. Crear una función que reciba una cadena de texto y retorne la cantidad de vocales que contiene.
 */

function cantidadDeVocales(cadena) {}

console.log(cantidadDeVocales("Hola Mundo Loco"));

/**
 * 7. Crear una función que reciba una cadena de texto y retorne true si es un palíndromo y false si no lo es.
 */

function esPalindromo(cadena) {}

console.log(esPalindromo("Anita lava la tina"));

/**
 * 8. Crear una función que reciba una cadena de texto y retorne true si es un número y false si no lo es.
 */

function esNumero(cadena) {}

console.log(esNumero("123"));
