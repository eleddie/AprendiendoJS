/**
 * 1. Crear una función que reciba un objeto y retorne el valor de la propiedad nombre.
 */

function obtenerNombre(objeto) {}

console.log(obtenerNombre({ nombre: "Juan", edad: 20 }));

/**
 * 2. Crear una función que reciba un objeto y agregue una propiedad llamada apellido.
 */

function agregarApellido(objeto) {}

console.log(agregarApellido({ nombre: "Juan" }));

/**
 * 3. Crear una función que reciba un objeto y elimine la propiedad edad.
 */

function eliminarEdad(objeto) {}

console.log(eliminarEdad({ nombre: "Juan", edad: 20 }));

/**
 * 4. Crear una función que reciba un objeto y muestre todas las propiedades en formato de lista.
 */

function mostrarPropiedades(objeto) {}

console.log(mostrarPropiedades({ nombre: "Juan", edad: 20 }));

/**
 * 5. Crear una función que reciba un objeto y muestre todos los valores en formato de lista.
 */

function mostrarValores(objeto) {}

console.log(mostrarValores({ nombre: "Juan", edad: 20 }));

/**
 * 6. Crear una función que reciba un objeto y lo duplique.
 */

function duplicarObjeto(objeto) {}

console.log(duplicarObjeto({ nombre: "Juan", edad: 20 }));
